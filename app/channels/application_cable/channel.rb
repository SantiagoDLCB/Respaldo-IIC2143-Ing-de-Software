# frozen_string_literal: true

# Clase abstracta de la que heredan todos los canales
module ApplicationCable

  # Clase que representa un canal de ActionCable
  class Channel < ActionCable::Channel::Base
  end
end
